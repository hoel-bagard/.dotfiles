# OpenCode Configuration

The agents in [agents](./agent/) have been created using the `opencode agent create` command, with some manual modifications.

See [the configuration docs](https://opencode.ai/docs/config) for explanations.
